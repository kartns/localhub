name: ðŸš€ Deploy to VPS

on:
  push:
    branches: [ Production ]  # Deploy only on Production branch
  workflow_dispatch: # Allow manual trigger

jobs:
  deploy:
    name: ðŸŒŸ Deploy LocalHub to thelocalhub.gr
    runs-on: ubuntu-latest
    environment: production  # Use production environment
    
    steps:
    - name: ðŸ“¥ Checkout code
      uses: actions/checkout@v4
      
    - name: ðŸ”§ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          frontend/package-lock.json
          backend/package-lock.json

    - name: ðŸ“¦ Install frontend dependencies
      run: |
        cd frontend
        npm ci

    - name: ðŸ“¦ Install backend dependencies
      run: |
        cd backend
        npm ci

    - name: ðŸ§ª Run frontend tests (if any)
      run: |
        cd frontend
        npm run test --if-present || echo "No frontend tests found"

    - name: ðŸ§ª Run backend tests (if any)
      run: |
        cd backend
        npm run test --if-present || echo "No backend tests found"

    - name: ðŸ—ï¸ Build frontend
      run: |
        cd frontend
        npm run build

    - name: âœ… Build success
      run: echo "âœ… Application built successfully!"

    - name: ðŸš€ Deploy to VPS
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        key: ${{ secrets.VPS_SSH_KEY }}
        port: 22
        script: |
          echo "ðŸŒŸ Starting deployment to thelocalhub.gr..."
          
          # Navigate to project directory
          cd /home/${{ secrets.VPS_USERNAME }}/projects/localhub || {
            echo "âŒ Project directory not found. Please clone the repository first:"
            echo "   git clone https://github.com/${{ github.repository }}.git localhub"
            exit 1
          }
          
          # Pull latest changes
          echo "ðŸ“¥ Pulling latest code..."
          git fetch origin
          git reset --hard origin/Production
          
          # Run deployment script
          echo "ðŸ³ Running deployment script..."
          chmod +x deploy.sh
          sudo ./deploy.sh
          
          # Check deployment status
          echo "ðŸ“Š Checking deployment status..."
          docker-compose -f docker-compose.proxy.yml ps
          
          echo "âœ… Deployment completed!"
          echo "ðŸŒ LocalHub is live at https://thelocalhub.gr"

    - name: ðŸŽ‰ Deployment Success
      if: success()
      run: |
        echo "âœ… Deployment successful!"
        echo "ðŸŒ Your LocalHub app is live at: https://thelocalhub.gr"

    - name: âŒ Deployment Failed
      if: failure()
      run: |
        echo "âŒ Deployment failed!"
        echo "Please check the logs above for details."
        echo "You can also manually deploy by SSH'ing to your VPS and running:"
        echo "   cd localhub && sudo ./deploy.sh"

