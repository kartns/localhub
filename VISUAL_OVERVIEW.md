# 🎬 Visual Project Overview

## 🏗️ Architecture Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                     Your Computer                           │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │            Web Browser (Port 3000)                   │   │
│  │         http://localhost:3000                        │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │  Food Storage Hub React App                   │  │   │
│  │  │  ┌──────────────────────────────────────────┐ │  │   │
│  │  │  │ - StorageList (Grid Layout)              │ │  │   │
│  │  │  │ - StorageCard (Individual Storage)       │ │  │   │
│  │  │  │ - StorageForm (Add New Storage)          │ │  │   │
│  │  │  │ - Styled with Tailwind CSS               │ │  │   │
│  │  │  │ - Mobile Responsive                      │ │  │   │
│  │  │  └──────────────────────────────────────────┘ │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
│                          │                                   │
│  HTTP Requests (REST API)│                                   │
│         GET/POST/        │                                   │
│      PUT/DELETE          │                                   │
│                          ▼                                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │        Express.js Server (Port 3001)                │   │
│  │      http://localhost:3001/api                      │   │
│  │                                                      │   │
│  │  Routes:                                            │   │
│  │  - GET /storages (all storages with counts)         │   │
│  │  - GET /storages/:id (single storage + items)       │   │
│  │  - POST /storages (create new storage)              │   │
│  │  - PUT /storages/:id (update storage)               │   │
│  │  - DELETE /storages/:id (delete storage)            │   │
│  │  - GET /health (server status)                      │   │
│  │                                                      │   │
│  └──────────────────────────────────────────────────────┘   │
│                          │                                   │
│  Database Queries        │                                   │
│                          ▼                                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │     SQLite Database (backend/data/storage.db)      │   │
│  │                                                      │   │
│  │  ┌─────────────┐  ┌──────────┐  ┌─────────────┐    │   │
│  │  │   Storages  │  │  Items   │  │ Categories  │    │   │
│  │  │             │  │          │  │             │    │   │
│  │  │ - id        │  │ - id     │  │ - id        │    │   │
│  │  │ - name      │  │ - name   │  │ - name      │    │   │
│  │  │ - address   │  │ - qty    │  │ - icon      │    │   │
│  │  │ - lat/long  │  │ - exp    │  │             │    │   │
│  │  │ - type      │  │ - cat    │  │ (Veggies... │    │   │
│  │  │ - desc      │  │ - date   │  │ Fruits...   │    │   │
│  │  └─────────────┘  └──────────┘  │ Grains...)  │    │   │
│  │                                   └─────────────┘    │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

```
User Action (Add Storage)
       │
       ▼
┌─────────────────────────────────┐
│ React Form (StorageForm.jsx)    │
│ - Collect user input            │
│ - Validate form data            │
└─────────────────────────────────┘
       │
       ▼ HTTP POST
┌─────────────────────────────────┐
│ Express API (storages.js)       │
│ - Receive POST request          │
│ - Validate data                 │
│ - Insert into database          │
└─────────────────────────────────┘
       │
       ▼ SQL INSERT
┌─────────────────────────────────┐
│ SQLite Database                 │
│ - Create new row in storages    │
│ - Auto-generate ID              │
│ - Set timestamps                │
└─────────────────────────────────┘
       │
       ▼ Return ID
┌─────────────────────────────────┐
│ Express API                     │
│ - Send 201 Created response     │
│ - Include new storage data      │
└─────────────────────────────────┘
       │
       ▼ JSON Response
┌─────────────────────────────────┐
│ React App                       │
│ - Receive new storage           │
│ - Update state                  │
│ - Re-render UI                  │
└─────────────────────────────────┘
       │
       ▼
 💾 New storage visible on page!
```

---

## 🎨 UI Component Hierarchy

```
App.jsx (Main Container)
│
├── Header Section
│   ├── Logo + Title
│   └── Subtitle
│
├── Action Bar
│   └── "+ Add Storage" Button
│
├── StorageForm.jsx (Conditional)
│   ├── Text Input (Name)
│   ├── Select Dropdown (Type)
│   ├── Textarea (Description)
│   ├── Text Input (Address)
│   ├── Number Input (Latitude)
│   ├── Number Input (Longitude)
│   ├── Button (Create)
│   └── Button (Cancel)
│
└── StorageList.jsx
    │
    └── StorageCard.jsx (x3 or more)
        ├── Storage Icon + Name
        ├── Location Badge
        ├── Description
        ├── Item Count Box
        ├── Coordinates
        ├── "View Details" Button
        └── "Delete" Button
```

---

## 📱 Responsive Breakpoints

```
Mobile (< 768px)              Tablet (768px - 1024px)    Desktop (> 1024px)
─────────────────             ──────────────────────     ──────────────────
┌────────────────┐            ┌──────────────────────┐   ┌────────────────────────────┐
│                │            │                      │   │                            │
│  Storage 1     │            │   Storage 1  Storage 2    │  Storage 1  Storage 2  Storage 3  │
│  ──────────    │            │   ────────   ────────     │  ──────────────────────────  │
│  • Title       │            │                      │   │                            │
│  • Location    │            │   Storage 3  Storage 4    │  Storage 4  Storage 5  Storage 6  │
│  • Items: 5    │            │   ────────   ────────     │  ──────────────────────────  │
│  • Buttons     │            │                      │   │                            │
│                │            │   Storage 5  Storage 6    │  ...                       │
│  ──────────    │            │   ────────   ────────     │  ──────────────────────────  │
│                │            │                      │   │                            │
│  Storage 2     │            │   ...                │   │  ...                       │
│  ──────────    │            │                      │   │                            │
│  • Title       │            └──────────────────────┘   └────────────────────────────┘
│  • Location    │
│  • Items: 3    │            1 Column                  2 Columns               3 Columns
│  • Buttons     │
│                │
│  ──────────    │
│                │
│  Storage 3     │
│  ──────────    │
│  ...           │
│                │
└────────────────┘
```

---

## 🔄 Data State Management

```
┌──────────────────────────────────────────────────────────────┐
│                    React State (App.jsx)                     │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ const [storages, setStorages] = useState([])          │  │
│  │ ↓ Gets populated by GET /api/storages                 │  │
│  │ ↓ Updates when POST/PUT/DELETE happens                │  │
│  │ ↓ Used to render StorageCard components               │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ const [showForm, setShowForm] = useState(false)        │  │
│  │ ↓ Toggle StorageForm visibility                        │  │
│  │ ↓ Reset after successful submission                    │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ const [loading, setLoading] = useState(true)           │  │
│  │ ↓ Show loading state while fetching                    │  │
│  │ ↓ Hide loading after data arrives                      │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 🎯 User Journey Map

```
Start
  │
  ▼
Open http://localhost:3000
  │
  ├─────────────────────────────┐
  │                             │
  ▼                             ▼
See Empty                    See Existing
Storage List                 Storages
  │                             │
  │◄─────────────────────────────┤
  │                             
  ▼                             
Click "+ Add Storage"
  │
  ▼
See StorageForm
  │
  ▼
Fill in Form
  │
  ├─ Cancel ──────┐
  │               │
  │ Submit        ▼
  │          Form Closes
  │
  ▼
Submit Form
  │
  ▼
API Creates
Storage
  │
  ▼
Page Refreshes
  │
  ▼
New Storage
Visible
  │
  ├──────────────┬──────────────┐
  │              │              │
  ▼              ▼              ▼
View Details  Keep Adding  Delete Storage
(Coming Soon) (Loop)       (Confirm)
  │              │              │
  │              │              ▼
  │              │         Storage Removed
  │              │              │
  └──────────────┴──────────────┘
                 │
                 ▼
             Manage Storages
                 ↓
             (Repeat as needed)
```

---

## 💾 Database Schema

```
storages
┌─────────────────────────────────────────────────────────┐
│ id (PRIMARY KEY)                                        │
│ name (TEXT, NOT NULL)                                   │
│ description (TEXT)                                      │
│ latitude (REAL)                                         │
│ longitude (REAL)                                        │
│ address (TEXT)                                          │
│ type (TEXT) → 'storage' | 'farm' | 'market' | 'warehouse'
│ created_at (DATETIME)                                   │
│ updated_at (DATETIME)                                   │
└─────────────────────────────────────────────────────────┘
       │ (One-to-Many)
       │
       ▼
items
┌─────────────────────────────────────────────────────────┐
│ id (PRIMARY KEY)                                        │
│ storage_id (FOREIGN KEY) → storages.id                 │
│ name (TEXT, NOT NULL)                                   │
│ category (TEXT)                                         │
│ quantity (INTEGER)                                      │
│ unit (TEXT)                                             │
│ expiration_date (DATE)                                  │
│ added_at (DATETIME)                                     │
│ updated_at (DATETIME)                                   │
└─────────────────────────────────────────────────────────┘

categories (lookup table)
┌─────────────────────────────────────────────────────────┐
│ id (PRIMARY KEY)                                        │
│ name (TEXT, UNIQUE)                                     │
│ icon (TEXT)                                             │
│                                                         │
│ Pre-populated:                                          │
│ - Vegetables (🥬)                                        │
│ - Fruits (🍎)                                            │
│ - Grains (🌾)                                            │
│ - Dairy (🧀)                                             │
│ - Proteins (🥚)                                          │
│ - Other (📦)                                             │
└─────────────────────────────────────────────────────────┘
```

---

## 🔌 API Endpoints Overview

```
┌─────────────────────────────────────────────────────────┐
│         REST API Endpoints (Port 3001)                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ GET /api/health                                         │
│ └─ Returns: {status: "OK", message: "..."}             │
│                                                         │
│ GET /api/storages                                       │
│ └─ Returns: [{id, name, type, item_count, ...}, ...]   │
│                                                         │
│ GET /api/storages/:id                                   │
│ └─ Returns: {id, name, ..., items: [{...}, ...]}       │
│                                                         │
│ POST /api/storages                                      │
│ ├─ Body: {name, description, address, lat, long, type}│
│ └─ Returns: Created storage object with id             │
│                                                         │
│ PUT /api/storages/:id                                   │
│ ├─ Body: {name, description, address, lat, long, type}│
│ └─ Returns: Updated storage object                     │
│                                                         │
│ DELETE /api/storages/:id                                │
│ └─ Returns: {message: "Storage deleted"}               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 Folder Structure Tree

```
food-storage-app/
│
├── 📘 Documentation
│   ├── INDEX.md                    ← Start here!
│   ├── GETTING_STARTED.md
│   ├── README.md
│   ├── QUICK_START.md
│   ├── SETUP_GUIDE.md
│   ├── API_TESTING.md
│   ├── TECH_REFERENCE.md
│   ├── PROJECT_SUMMARY.md
│   └── COMPLETION_SUMMARY.md
│
├── .gitignore
│
├── backend/
│   ├── package.json               ← Dependencies
│   ├── .env                        ← PORT=3001
│   └── src/
│       ├── index.js                ← Server entry (Starts on :3001)
│       ├── database.js             ← SQLite setup (Creates tables)
│       └── routes/
│           └── storages.js         ← API endpoints (CRUD)
│
└── frontend/
    ├── package.json                ← Dependencies
    ├── index.html                  ← HTML template
    ├── vite.config.js              ← Vite config (Port 3000, API proxy)
    ├── tailwind.config.js          ← Tailwind theme
    ├── postcss.config.js           ← PostCSS plugins
    └── src/
        ├── main.jsx                ← React DOM render
        ├── App.jsx                 ← Main component (Logic & layout)
        ├── index.css               ← Tailwind styles
        └── components/
            ├── StorageForm.jsx     ← Add storage form
            ├── StorageList.jsx     ← Grid wrapper
            ├── StorageCard.jsx     ← Individual card
            └── SampleCard.jsx      ← Example card
```

---

## ✨ Color Scheme

```
Primary Color (Green)          Secondary Color (Amber)
#10b981                        #f59e0b
▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓             ▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒

Used for:                      Used for:
- Buttons                      - Accent buttons
- Headers                      - Secondary actions
- Links                        - Emphasis
- Primary actions

Background Gradient:
Green-50 ────────→ Blue-50
Light ─────────────────────→ Light
(Creates soft, professional look)

Text Colors:
- Primary: gray-800
- Secondary: gray-600
- Light: gray-500
- Disabled: gray-400
```

---

## 🎮 Interactive Elements

```
Button States:
┌─────────────────────────────────────────────┐
│ Default           Hover           Active    │
│ bg-primary        bg-green-700    scale-95  │
│ text-white        shadow-lg       opacity-90│
│ transition        transform       transition│
└─────────────────────────────────────────────┘

Input States:
┌─────────────────────────────────────────────┐
│ Default           Focus           Error     │
│ border-gray-300   ring-primary    ring-red  │
│ bg-white          border-primary  border-red│
│ text-gray-900     text-gray-900   text-gray│
└─────────────────────────────────────────────┘

Card States:
┌─────────────────────────────────────────────┐
│ Default           Hover                     │
│ shadow-md         shadow-lg                 │
│ border-0          border-0                  │
│ transition        scale-105                 │
└─────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture (Future)

```
Internet
   │
   ▼
Netlify/Vercel (Frontend)
   │
   ├─ React app (compiled)
   ├─ Tailwind CSS (built)
   └─ JavaScript bundle
       │
       HTTP Requests
       │
       ▼
Railway/Vercel (Backend)
   │
   ├─ Express API
   ├─ Node.js server
   └─ Environment variables
       │
       Database queries
       │
       ▼
PostgreSQL/MongoDB Atlas
   │
   └─ Live database
```

---

This visual overview shows the complete architecture of your Food Storage Hub application!

**Ready to start? Open GETTING_STARTED.md!** 🚀
